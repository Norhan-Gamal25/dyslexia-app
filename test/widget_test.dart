import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Verbix app builds', (WidgetTester tester) async {
    // Placeholder test. The app requires Firebase initialization,
    // which is exercised at runtime rather than in unit tests.
    expect(1 + 1, 2);
  });
}